{
  "name": "kd-pixel-panel",
  "displayName": "KD Pixel Panel",
  "description": "Native IDE panel for KD observer events.jsonl",
  "version": "0.1.0",
  "publisher": "krackeddevs",
  "license": "MIT",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Visualization",
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished",
    "onCommand:kdPixel.openPanel",
    "onCommand:kdPixel.refreshPanel"
  ],
  "main": "./extension.js",
  "contributes": {
    "commands": [
      {
        "command": "kdPixel.openPanel",
        "title": "KD: Open Pixel Observer Panel"
      },
      {
        "command": "kdPixel.refreshPanel",
        "title": "KD: Refresh Pixel Observer Panel"
      }
    ],
    "configuration": {
      "title": "KD Pixel Panel",
      "properties": {
        "kdPixel.autoOpen": {
          "type": "boolean",
          "default": false,
          "description": "Automatically open KD Pixel panel after startup if a KD workspace is detected."
        },
        "kdPixel.eventsPath": {
          "type": "string",
          "default": ".kracked/runtime/events.jsonl",
          "description": "Path to KD events stream (absolute or workspace-relative)."
        },
        "kdPixel.refreshIntervalMs": {
          "type": "number",
          "default": 1500,
          "minimum": 500,
          "maximum": 10000,
          "description": "Polling interval in milliseconds."
        }
      }
    }
  }
}
